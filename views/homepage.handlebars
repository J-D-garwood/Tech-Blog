{{#each posts as |post|}}
<div>
    <a
      href='/post/{{post.id}}'
    >
        <div>
                <div>
                    <div>
                        <h3>{{post.title}}</h3>
                        <p>{{post.contents}}</p>
                    </div>
                </div>
            </div>
        </div>
    </a>
</div>
{{/each}}